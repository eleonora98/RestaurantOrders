<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.restaurantorders">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyTheme">
        <activity android:name=".FoodMenu.FoodMenuActivity"></activity>
        <activity android:name=".Restaurants.RestaurantsActivity" />
        <activity android:name=".ContactUs.ContactsActivity" />
        <activity
            android:name=".FoodMenu.FoodMenuActivity2"
            android:label="@string/title_activity_food_menu"
            android:theme="@style/Theme.RestaurantOrders.NoActionBar" />
        <activity android:name=".Orders.OrderActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>